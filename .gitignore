# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (root-level — worker/ has its own too)
node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Local env (never commit secrets)
.env
.env.local
documents_old/

# Authoring scratchpads — live data ships in app/public/data/
working_files/

# Vite build output (app/dist/) is produced by GitHub Actions, not committed
app/dist/
